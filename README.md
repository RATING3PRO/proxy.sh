# proxy.sh

## BBR enable/disable

enable

```bash
sudo bash <(curl -fsSL https://raw.githubusercontent.com/RATING3PRO/proxy.sh/main/bbr.sh) enable
```

disable

```bash
sudo bash <(curl -fsSL https://raw.githubusercontent.com/RATING3PRO/proxy.sh/main/bbr.sh) disable
```

status

```bash
sudo bash <(curl -fsSL https://raw.githubusercontent.com/RATING3PRO/proxy.sh/main/bbr.sh) status
```

## VLESS + REALITY + XTLS-RPRX-Vision

### alpine

install

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/RATING3PRO/proxy.sh/refs/heads/main/alpine-vless-reality-xtls-rprx-vision.sh)
```

uninstall

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/RATING3PRO/proxy.sh/refs/heads/main/alpine-vless-reality-xtls-rprx-vision.sh) uninstall
```
### Ubuntu / Debian

install

```bash
sudo bash <(curl -fsSL https://raw.githubusercontent.com/RATING3PRO/proxy.sh/main/ubuntu-vless-reality-xtls-rprx-vision.sh)
```

uninstall

```bash
sudo bash <(curl -fsSL https://raw.githubusercontent.com/RATING3PRO/proxy.sh/main/ubuntu-vless-reality-xtls-rprx-vision.sh) uninstall
```

### RedHat / CentOS / Alma / Rocky / Oracle

install

```bash
sudo bash <(curl -fsSL https://raw.githubusercontent.com/RATING3PRO/proxy.sh/main/redhat-vless-reality-xtls-rprx-vision.sh)
```

uninstall

```bash
sudo bash <(curl -fsSL https://raw.githubusercontent.com/RATING3PRO/proxy.sh/main/redhat-vless-reality-xtls-rprx-vision.sh) uninstall
```

## VLESS + REALITY + XHTTP

### alpine

install

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/RATING3PRO/proxy.sh/refs/heads/main/alpine-vless-reality-xhttp.sh)
```

uninstall

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/RATING3PRO/proxy.sh/refs/heads/main/alpine-vless-reality-xhttp.sh) uninstall
```

### Ubuntu / Debian

install

```bash
sudo bash <(curl -fsSL https://raw.githubusercontent.com/RATING3PRO/proxy.sh/main/ubuntu-vless-reality-xhttp.sh)
```

uninstall

```bash
sudo bash <(curl -fsSL https://raw.githubusercontent.com/RATING3PRO/proxy.sh/main/ubuntu-vless-reality-xhttp.sh) uninstall
```

### RedHat / CentOS / Alma / Rocky / Oracle

install

```bash
sudo bash <(curl -fsSL https://raw.githubusercontent.com/RATING3PRO/proxy.sh/main/redhat-vless-reality-xhttp.sh)
```

uninstall

```bash
sudo bash <(curl -fsSL https://raw.githubusercontent.com/RATING3PRO/proxy.sh/main/redhat-vless-reality-xhttp.sh) uninstall
```